# Служебные папки IDE и ассистента
.claude/
.idea/

# Python
__pycache__/
*.pyc
venv/
yolo/

# Веса модели (скачиваются автоматически, ~5 МБ, не храним в git)
*.pt

# Стандартная папка вывода ultralytics (используем свою — results/)
runs/

# Секреты — токен DeGirum (НЕ должен попасть в публичный репозиторий)
degirum_token.txt

# Системное
.DS_Store

# Шаблон отчёта по практике (LaTeX) — личный, не коммитим
шаблон_оформления_отчёта_по_практике_в_latex/
